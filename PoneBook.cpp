#include <iostream>
#include <string>
//#include <cstring>
#include <fstream>
#include <Windows.h>
#include "InterFullName.h"
using namespace std;

int main()
 {
    setlocale(0, "");
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    MENU menu;
   /* INTERFULLNAME fullname;
    WRITE write(fullname);
    READ read(fullname);*/
   /* cout << "Inter 1 or 2 : ";
    int choice;
    cin >> choice;
    switch (choice)
    {
    case 1:
        fullname;
    case 2:
        write;
    case 3:
        read;
    default:
        break;
    }*/

    return 0;

}